# Ensure Linux-targeted files always use LF line endings
*.sh text eol=lf
Dockerfile text eol=lf
docker-compose.yml text eol=lf

# Contract snapshots are regenerated by tests that always write LF. Pinning the checkout keeps
# a regenerated snapshot from showing up as a whole-file diff on Windows.
*.snapshot.json text eol=lf
